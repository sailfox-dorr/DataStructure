package com.scala.HerfmanTree

object HuffmanTree {
    //节点 的带权路径的长度
    //路径的长度和权放入乘积
    //节点的长度 层数

    // 书的带权路径
    //所有的带权路径的个wpl最小
    def main(args: Array[String]): Unit = {
        // 将一个数列转化为一个huffman树
        // 将list
        val arr: Array[Int] = Array(1, 5, 2, 6, 3, 5, 4)
//        arr.map((i: Int) => new Node(i))
    }



}
